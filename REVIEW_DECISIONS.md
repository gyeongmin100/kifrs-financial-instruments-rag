# 리뷰 결정사항

리뷰 항목을 하나씩 검토하며 확정한 구현 방향을 기록한다.

> **§2는 현재 코드와 다르다.** 2026-07-29에 RAG 파이프라인을 단순화하면서 `deterministic_failed`·`semantic_insufficient`·`semantic_error`·`semantic_not_run`을 포함한 판정 단계 전체를 제거했다. 당시 판단 근거로만 남겨 둔다. 현재 상태 계약은 §7을 참고한다.

## 1. 폴링 무한 루프

- 상태: 구현 완료
- 정상적인 `pending` 응답에는 전체 횟수 제한을 두지 않는다.
- 네트워크 오류처럼 HTTP 응답 자체를 받지 못한 **연속 실패**만 센다.
- 정상 응답을 한 번이라도 받으면 연속 실패 횟수를 0으로 초기화한다.
- 20회 연속 실패하면 폴링과 스피너를 중단하고 `일시적인 오류가 발생했습니다.` 안내 및 `다시 시도` 버튼을 표시한다. 상태별 색상이나 기술적인 연결 오류 설명은 노출하지 않는다.
- `다시 시도`는 질문을 새로 POST하지 않고 기존 `jobId`의 상태 조회만 재개한다. 중복 작업 생성을 방지하기 위함이다.

## 2. 응답 상태 표시

- 상태: 구현 완료
- `deterministic_failed`는 후보 수, 고유 근거 ID, 본문·인용·점수 존재 여부와 임계치 충족 여부를 코드 규칙으로 판정한다. 단, 관련성·직접성 점수 자체는 OpenAI reranker가 생성한다.
- `semantic_insufficient`는 OpenAI semantic judge가 질문 항목별 근거 충족 여부, 누락 항목, 충분성, 신뢰도를 구조화 출력하고, 코드가 신뢰도 0.65 이상·모든 항목 충족·누락 없음 조건을 다시 적용한 결과다.
- 근거 판정에는 `sufficient`, `deterministic_failed`, `semantic_insufficient` 외에도 `semantic_not_run`, `semantic_error`가 있다.
- 파이프라인 최종 상태는 주로 `answered`, `insufficient`, `generation_failed`이며, 현재 일반 API 응답은 내부 근거 판정 상세 상태를 노출하지 않는다.
- 따라서 프런트엔드에서 상세 상태별로 안정적으로 분류하려면 API에 별도의 명시적 사유 필드가 필요하다. `conclusion` 문구를 파싱해 분류하지 않는다.
- 사용자 화면에는 상태 배지나 상태별 색상을 사용하지 않는다.
- 정상 답변은 `답변` 아래에 conclusion, `근거` 아래에 reasoning 번호 목록, `참조` 아래에 evidence 목록을 표시한다.
- `deterministic_failed`, `semantic_insufficient`, `generation_failed`는 사용자 화면에서 구분하지 않고 `현재 정보만으로는 신뢰성 있는 답변을 제공하기 어렵습니다.`만 표시한다. 내부 상태값은 구분해서 유지한다.
- `semantic_error`와 `semantic_not_run` 등 근거 판정 시스템 오류에는 `일시적인 오류가 발생했습니다.`와 `다시 시도` 버튼을 표시한다.

## 3. 전송 실패 시 질문 중복

- 상태: 구현 완료
- 전송 버튼을 누르면 사용자 말풍선을 즉시 표시하고 입력창과 첨부 영역을 즉시 비운다.
- 질문과 첨부 이미지는 재시도를 위해 해당 요청의 내부 데이터로 보관한다.
- POST 실패 시 `일시적인 오류가 발생했습니다.`와 `다시 시도` 버튼을 표시한다.
- `다시 시도`는 새 사용자 말풍선이나 대화 기록을 만들지 않고 기존 요청 데이터로 다시 POST한다. 기존 오류 영역은 처리 중 표시로 교체하고, 성공하면 같은 흐름에서 답변을 표시하며, 다시 실패하면 오류와 버튼을 복원한다.
- POST는 자동 재시도하지 않고 사용자가 버튼을 눌렀을 때만 재전송한다.
- 각 사용자 요청에 고유한 `requestId`를 부여하고 재시도할 때 동일한 값을 사용한다. 서버는 같은 `requestId`에 새 작업을 만들지 않고 기존 `jobId`를 반환해 중복 작업을 방지한다.

## 4. 모바일 disclaimer 가로 넘침

- 상태: 구현 완료
- 포트폴리오용 화면이므로 disclaimer 문구를 완전히 제거한다.
- `index.html`의 disclaimer 요소를 제거하고, 더 이상 사용되지 않는 `.disclaimer` CSS 규칙도 함께 제거한다.

## 5. favicon 없음

- 상태: 구현 완료 (초기 구현이 프로덕션에서 동작하지 않아 후속 수정함)
- 사용자가 제공한 `favicon.png`를 정적 자산으로 사용한다.
- API의 `/favicon.ico` 경로는 동일한 PNG 파일을 반환한다.
- HTML은 `/favicon.ico`가 아니라 `/static/favicon.png`를 연결한다.
  프론트엔드는 Cloudflare Pages 정적 호스팅이라 FastAPI의 `/favicon.ico` 라우트가 존재하지
  않는다. 초기 구현은 로컬(FastAPI가 직접 서빙)에서만 동작했고 실제 배포에서는 404였다.
- `scripts/build_frontend_dist.py`의 복사 목록에 `favicon.png`를 포함한다. 누락 시 번들에
  파일이 들어가지 않는다.
- 원본 이미지는 1024×1024 / 1.32 MB였다. 파비콘 하나에 매 방문 1.32 MB를 전송하게 되므로
  180×180 / 8.9 KB로 축소해 배포하고, 원본은 프로젝트 루트 `favicon.png`에 보존한다.

## 6. HTTP 오류 중 사용자 조치가 필요한 상태

- 상태: 구현 완료
- `errorText` 제거로 모든 HTTP 오류가 공통 오류와 재시도 버튼으로 통일되었으나, 429와 422는
  서버 장애가 아니라 사용자가 행동을 바꿔야 하는 상태다. 같은 요청을 재전송하면 반드시 실패한다.
- 특히 429는 job이 생성되기 전에 거절되므로 `request_id` 멱등 보호를 받지 못한다. 재시도할수록
  rate limit을 추가 소비해 상황이 악화된다.
- `renderHttpError`를 도입해 429·422만 안내 문구를 표시하고 재시도 버튼을 주지 않는다.
  그 외 상태는 기존 결정대로 내부 원인을 숨긴 공통 오류로 처리한다.
- 네트워크 실패는 `status`가 없으므로 공통 오류 경로를 그대로 탄다.

## 검증 결과

- Python 전체 테스트: 134개 통과 (기존 132 + favicon·HTTP 오류 회귀 방지 2)
- JavaScript 문법 검사: 통과 (원본 및 배포 번들)
- 독립 검증 에이전트 재감사: 최종 통과
- 실제 로컬 브라우저에서 정상 답변, 근거 부족, 생성 검증 실패, 시스템 오류와 재시도, 오류 요청 복원·폐기 동작을 확인했다.
- 프로덕션(`https://accounting-rag.pages.dev`) 확인:
  `/static/favicon.png` → 200 / `image/png` / 8,889 bytes,
  배포된 `app.js`가 로컬 번들과 바이트 단위 동일.

## 이후 작업 시 주의

- 로컬 검증만으로 프론트엔드 결론을 내지 말 것. 로컬은 FastAPI가 정적 파일과 라우트를 함께
  제공하지만 실제 프론트엔드는 라우트가 없는 정적 호스팅이다.
- `static/`에 자산을 추가하면 `scripts/build_frontend_dist.py`의 복사 목록에도 추가할 것.

## 7. RAG 파이프라인 단순화 (2026-07-29)

- 상태: 구현 완료·배포 완료
- 배경: 라이브 백엔드에 질문 6개를 던져 **4개가 기각**됐다. "위험회피회계를 적용하기 위한 요건은?" 같은 핵심 질문도 포함됐다.

### 확인한 원인

1. **의미 판정 프롬프트가 완전성을 요구했다.** `"Return sufficient=true only when all material claims, conditions, and exceptions ... are covered"`. K-IFRS는 어떤 주제든 예외 규정이 딸려 있어 상위 10개 청크로는 충족 불가능하다.
2. **같은 질문이 실행마다 다른 결과를 냈다.** 기계 판정이 재정렬 LLM의 점수에 의존했는데 5개 LLM 호출 어디에도 `temperature`·`seed`·`top_p`가 없었다. 동일 질문이 1차 `deterministic_failed`, 2차 `answered`로 갈렸다.

### 결정

임계값 재조정 대신 **판정 단계 자체를 제거**한다. 답변 생성 프롬프트에 이미 자체 거절 규칙(5·6번)이 있으므로 그 신호 하나로 대체한다.

- 제거: `reranker.py` · `sufficiency.py` · `citation_verifier.py` · `graph_expansion.py` · `query/analysis.py` · `config/query.yaml` (약 1,400줄)
- 유지(당시): `generation/answer.py`의 `_validate_answer()`. 인용 검증을 "없앤" 것이 아니라 생성 직후로 옮긴 것이었다. **§8에서 이 결정을 다시 뒤집었다.**
- 추가: `DERIVED_FROM` 형제 청크 보강. 하위항목 계층은 청킹에서 이미 해결돼 있고, 실제 빈틈은 크기 때문에 쪼개진 문단(3.4%)이었다.

### 상태 계약 (당시 — 조건은 §8에서 바뀜)

| status | reason | 조건 |
|---|---|---|
| `answered` | `sufficient` | 검증 통과 + 거절 접두사 없음 |
| `insufficient` | `self_declined` | 모델 자체 거절 또는 검증 실패 fallback |
| `insufficient` | `no_evidence_found` | 검색 결과 0건 |

status·reason 값 자체는 지금도 같다. 판정 조건만 §8에서 바뀌었다.

`generation_failed` 상태와 나머지 reason은 제거했다. 프론트엔드 `resultKind()`는 구버전 값도 인식하도록 두어 배포 순서가 어긋나도 화면이 깨지지 않는다.

### 결과

LLM 호출 최대 12회 → 1회, 거절 게이트 4개 → 0개, 답변 성공 **2/6 → 5/6**. 범위 밖 질문("오늘 서울 날씨")은 여전히 거절한다.

### 함께 고친 것

`AnswerConfig.max_candidates`가 10이었는데 검색이 최대 20개(top_k 12 + siblings 8)를 반환하게 되어, **뒤에 붙는 형제 청크가 잘려 모델에 도달하지 못하는 상태**였다. 20으로 올리고 `config/answering.yaml`에 노출했다.

### 이후 주의사항

1. `reason` 값을 바꾸면 `api/schemas.py`와 `static/app.js`의 `resultKind()`를 함께 바꾼다. 프론트엔드를 먼저 배포한다.
2. `conclusion` 문자열로 상태를 판별하지 않는다.
3. `max_candidates >= top_k + max_siblings`를 유지한다.
4. 게이트를 다시 추가하려면 기각률을 먼저 측정한다. 위 6개 질문 세트가 기준선이다.

---

## 8. 답변 검증 제거와 출처 조립 방식 전환

### 배경

§7에서 게이트 4개를 제거하면서 `_validate_answer()`는 "남은 유일한 방어선"으로 유지했다. 그 판단이 옳았는지 질문 14개로 측정했다.

### 측정 결과

거절 4건 중 **3건이 진짜 거절이 아니었다.**

| 질문 | 모델이 실제로 한 일 | 죽은 이유 |
|---|---|---|
| 법인세비용 계산 | 근거 3개 인용, "제1012호 소관"이라 정확히 안내 | 문장 하나에 `[E1]` 마커 없음 |
| 오늘 서울 날씨 | "제공된 근거는 K-IFRS 금융상품 내용"이라 구체적 거절 | `답변할 수 없습니다`가 접두사 목록에 없음 |
| 금융부채·지분상품 구분 | 5,149자 작성 중 잘림 | `max_output_tokens` 초과 (검사 아님) |

셋 다 사용자에게는 동일한 `근거 부족: 검증된 답변을 생성하지 못했습니다.`로 표시됐다.

### 결정

**검사를 고치는 대신 검사할 필요를 없앤다.**

`citation`과 `statement`는 검색 단계에서 `prepare_evidence_catalog()`가 이미 만든 값이다. 모델에게 되물으니 틀릴 수 있었고, 틀릴 수 있으니 검사가 필요했다. 스키마에서 두 필드를 빼고 `evidence`를 `["E1","E3"]` 형태의 번호 목록으로 바꿨다.

- `_validate_answer()` · `_validate_narrative()` 제거 → `_assemble_answer()` 신설
- 모르는 번호는 그 항목만 제외하고 답변은 유지. 폐기 경로 없음
- 선언 목록과 본문 마커를 **합집합**으로 모음 (한쪽에만 있는 근거가 카드에서 사라지지 않게)
- `_fallback()`은 API 오류와 JSON 파싱 실패에만 남음
- `max_output_tokens` 3000 → 8000. 추론 모델은 생각한 토큰도 이 한도에 포함한다

거절 판정도 바꿨다. 접두사만 믿으면 모델이 다른 표현으로 거절했을 때 `answered`로 잘못 분류되므로, **근거를 하나도 인용하지 않았는지**를 1순위 신호로 둔다.

```python
declined = not (answer.get("evidence") or []) or conclusion.startswith(_INSUFFICIENT_PREFIXES)
```

### 결과

| | 전 | 후 |
|---|---|---|
| 답변 | 10/14 | **11/14** |
| 검사로 폐기 | 3건 | **0건** |
| 근거 카드 수(질문당 평균) | 2.2 | **2.9** |

`max_output_tokens` 상향으로 "금융부채와 지분상품 구분"(1032호 핵심 주제)이 거절 → 답변으로 바뀌었다.

### 함께 검토했으나 도입하지 않은 것 — 2차 재검색

근거 부족 시 `REFERS_TO` 확장 후 재생성하는 안. **측정 결과 도입하지 않았다.**

- 1-hop 확장은 청크의 65%에서 결과가 0개, 평균 1.7개, 최대 232개
- `REFERS_TO`는 Paragraph에서 나가지 않는다(0개). 출발은 Block·Table·Footnote이므로 **2-hop은 `HAS_BLOCK` 구조 관계를 경유해야 하고, 의미상 "참조의 참조"가 아니다**
- 거절 4건 중 재검색이 구제 가능한 것은 1건. 나머지는 검사 실패와 토큰 초과였다
- 시드 12개 기준 평균 +19.8개를 붙이면 후보가 `max_candidates`(20)를 넘고, 이미 출력 토큰이 모자란 상태에서 컨텍스트를 키우는 셈이다

상세한 측정치는 `PROJECT_STRUCTURE.md` §4 "검토했으나 도입하지 않은 것"에 있다.

### 이후 주의사항

1. `evidence` 스키마를 되돌려 `citation`을 다시 받게 하면 검증이 필요해진다. 되돌리기 전에 이 절을 읽을 것.
2. 거절률이 높아 보이면 **원인부터 분류한다.** `evidence`가 비었으면 범위 밖, 근거가 있는데 거절이면 부분 부족이다. 후자의 비율을 재기 전에 검색을 손대지 않는다.
3. `max_output_tokens`는 한도일 뿐 사용량이 아니다. 낮춰서 비용을 아끼려 하면 JSON이 잘려 답변 전체를 잃는다.
