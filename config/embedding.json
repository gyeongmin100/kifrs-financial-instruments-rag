{
  "dimensions": 3072,
  "batch_size": 64,
  "api_limits_note": "OpenAI embeddings accept array input, at most 2048 inputs, 8192 tokens per input, and 300000 aggregate tokens per request. Current contextualized chunks are at most 1945 characters, so batch 64 is deliberately conservative without a tokenizer."
}
